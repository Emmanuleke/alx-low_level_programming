#include <stdio.h>
/**
 * main - this prints its name
 * @argc: parameter
 * @argv: parameter
 * Return: 0 for success
 */
int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", *argv);
return (0);
}
