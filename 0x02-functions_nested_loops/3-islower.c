#include "main.h"
/**
 * _islower- this code checks for lowercase characters
 * code goes hee
 * Return: if lowecase (1) if not (0)
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
return (1);
else
return (0);
}
