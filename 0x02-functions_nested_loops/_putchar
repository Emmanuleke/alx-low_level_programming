#include <unistd.h>
/**
*_putchar- write the characte c to stout
*@c: the character to point 
*Return: on success 1
*on error, -1 is return and error is set appropriately
*/
int _putchar(char c)
{
return (write(1, &c, 1));
}
